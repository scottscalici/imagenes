<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>plan de clase</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="font-sans bg-gray-100 min-h-screen p-6">
  <div class="max-w-5xl mx-auto space-y-6">

<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Diagrama de Flujo del Pretérito · Prototipo v2</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    @media print {
      details {display: block;}
      .no-print {display:none}
      .card {box-shadow:none; border-color:#000}
    }
  </style>
</head>
<body class="bg-gray-50 text-gray-900">
  <main class="max-w-5xl mx-auto p-6 space-y-6">
    <header class="bg-white border border-gray-200 rounded-2xl p-6 shadow card">
      <h1 class="text-3xl font-bold">Diagrama de Flujo del Pretérito</h1>
      <p class="mt-2 text-sm text-gray-600">Identifica qué patrón aplica a un verbo para conjugarlo correctamente en el pretérito.</p>
      <div class="mt-4 flex flex-wrap gap-2 text-xs">
        <span class="px-2 py-1 bg-red-50 text-red-700 border border-red-200 rounded-full">🔻 Irregular total</span>
        <span class="px-2 py-1 bg-orange-50 text-orange-700 border border-orange-200 rounded-full">🟠 Cambio ortográfico / i→y</span>
        <span class="px-2 py-1 bg-blue-50 text-blue-700 border border-blue-200 rounded-full">🔵 Cambio de raíz (e→i / o→u)</span>
        <span class="px-2 py-1 bg-emerald-50 text-emerald-700 border border-emerald-200 rounded-full">🟢 Regular</span>
      </div>
    </header>

    <!-- FUI / DI / VI section fix -->
    <section class="bg-white border border-gray-200 rounded-2xl p-6 shadow card">
      <h2 class="text-xl font-bold mb-4">START → ¿Qué tipo de verbo es?</h2>

      <div class="border-b border-gray-200 pb-4 mb-4">
        <p class="font-semibold text-lg">¿Es <em>ir, ser, dar</em> o <em>ver</em>? <span class="text-red-600 text-sm">(Irregular total · sin acentos)</span></p>
      </div>

      <div class="grid md:grid-cols-3 gap-4">
        <div class="p-4 bg-red-50 border border-red-200 rounded-xl flex flex-col items-center justify-center text-center">
          <p class="font-semibold text-gray-800 mb-1">IR / SER</p>
          <p class="text-sm">fui, fuiste, fue, fuimos, fuisteis, fueron</p>
        </div>
        <div class="p-4 bg-red-50 border border-red-200 rounded-xl flex flex-col items-center justify-center text-center">
          <p class="font-semibold text-gray-800 mb-1">DAR</p>
          <p class="text-sm">di, diste, dio, dimos, disteis, dieron</p>
        </div>
        <div class="p-4 bg-red-50 border border-red-200 rounded-xl flex flex-col items-center justify-center text-center">
          <p class="font-semibold text-gray-800 mb-1">VER</p>
          <p class="text-sm">vi, viste, vio, vimos, visteis, vieron</p>
        </div>
      </div>
    </section>

    <!-- Additional sections placeholder -->
    <section class="bg-white border border-gray-200 rounded-2xl p-6 shadow card">
      <p class="text-sm text-gray-600 italic">Resto del diagrama sigue igual (morphing, -AR, -ER, -IR)...</p>
    </section>

  </main>
</body>
</html>





  </div>

 
  <!-- Shared JavaScript -->
  <script>

  // Conversación timer
 let timerInterval;
let seconds = 0;

function startTimer(btn) {
  const timerDisplay = btn.parentElement.querySelector("[data-timer]");
  clearInterval(timerInterval);
  timerInterval = setInterval(() => {
    seconds++;
    const mins = String(Math.floor(seconds / 60)).padStart(2, '0');
    const secs = String(seconds % 60).padStart(2, '0');
    timerDisplay.textContent = `${mins}:${secs}`;
  }, 1000);
}

function resetTimer(btn) {
  const timerDisplay = btn.parentElement.querySelector("[data-timer]");
  clearInterval(timerInterval);
  seconds = 0;
  timerDisplay.textContent = "00:00";
}
    function toggleVocab() {
      const box = document.getElementById("vocabBox");
      const btn = document.getElementById("toggleBtn");
      if (box.classList.contains("hidden")) {
        box.classList.remove("hidden");
        btn.innerText = "Reducir";
      } else {
        box.classList.add("hidden");
        btn.innerText = "Expandir";
      }
    }
  </script>
</body>
</html>
